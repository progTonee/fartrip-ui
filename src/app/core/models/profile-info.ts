import { Car } from './car';

export interface ProfileInfo {
  age: string;
  car: Car;
  email: string;
  id: number;
  name: string;
  rating: number;
  status: string;
  workDescription: string;
}
